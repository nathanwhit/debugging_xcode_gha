# .github/workflows/basic.yml
name: Basic Workflow

on: push

jobs:
  build:
    runs-on: macos-14
    steps:
      - run: xcode-select --print-path
      - run: python find_sdk.py 15 --print_sdk_path
